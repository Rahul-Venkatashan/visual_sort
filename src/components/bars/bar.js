import styles from './bar.module.css'

export default function Bar({height}){
    
    // let [height, width] = [Math.random() * (max_height - 5) + (5), (100/max_height)]
    // console.log(`Bar was generated with height: ${height} and width: ${width}`)
    // return <div className={styles.bar} style={{
    //     height: `${height}vh`,
    //     width: `${width}%`
    // }}></div>    
}